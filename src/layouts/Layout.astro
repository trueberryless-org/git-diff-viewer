---
const { title = "Git Diff Viewer" } = Astro.props;
---

<html lang="en" class="dark">
  <head>
    <meta charset="utf-8" />
    <title>{title}</title>
    <style>
      body {
        background: #0d0d0d;
        color: #f5f5f5;
        font-family: monospace, monospace;
        padding: 2rem;
        color-scheme: dark;
      }
      input, button {
        background: #1a1a1a;
        border: 1px solid #333;
        color: #f5f5f5;
        padding: 0.5rem;
        margin: 0.5rem 0;
        border-radius: 0.25rem;
      }
      button {
        cursor: pointer;
      }
    </style>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/diff2html/bundles/css/diff2html.min.css"
    />
  </head>
  <body>
    <slot />
  </body>
</html>
